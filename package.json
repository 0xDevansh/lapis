{
  "name": "lapis",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm --filter web run build && pnpm --filter worker run build",
    "deploy": "pnpm --filter worker run deploy",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.5.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "workerd", "sharp"]
  }
}
