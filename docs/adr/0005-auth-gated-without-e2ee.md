# Auth-Gated Vaults Without End-To-End Encryption

The first slice of Lapis uses auth-gated private vault access rather than end-to-end encryption. End-to-end encryption would make browser rendering, search, backlinks, patch merging, and server-side conflict handling substantially harder, so the open-source self-deployed product starts with normal authenticated private storage and leaves E2EE outside the first slice.
