# Open-Source Self-Deployable Product

Lapis is an open-source product intended to be deployed by a Vault Owner or operator into their own Cloudflare account, not a first-party hosted SaaS with built-in pricing. The application therefore avoids product-level billing and default quota enforcement, while allowing deployment configuration to set optional upload or storage limits when an operator wants them.
